import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#99de01',
        },
        secondary: {
            main: "#acabab",
            light: "#ffffff",
            dark: "#0a0b0d"
        },
    },
});

export default theme